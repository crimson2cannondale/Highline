//Inventory button
var invButton = document.createElement('button');
invButton.innerText = 'inventory'
invButton.id = 'invButton' 
      
invButton.addEventListener('click', () => {
  document.write(inventory)
})
      
document.body.appendChild(invButton);
]document.write("<br>");
